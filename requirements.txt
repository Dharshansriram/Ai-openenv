# requirements.txt — Adaptive AI Project Manager v2.1
# Compatible with Python 3.10+ (3.11/3.12 recommended).
# Uses loose version ranges so pip always picks a pre-built wheel
# — avoids the pydantic-core Rust compilation error on Python 3.13/3.14.

fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
pydantic>=2.7.0,<3.0.0
httpx>=0.27.0,<1.0.0        # async HTTP client (for testing)
anyio>=4.3.0,<5.0.0   # async I/O foundation
requests
openai